#include <iostream>
#include </usr/include/SDL2/SDL.h>
#include "Logger.h"

using namespace std;

int main() {

	string logback = "logback.xml";
	cout << "Logger" << endl;
	Logger *log = new Logger();
	return 0;
}
